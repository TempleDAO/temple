{
  "rewrites": [{ "source": "/(.*)", "destination": "/" }],
  "builds": [{ "src": "./dist/**", "use": "@vercel/static" }],  
  "github": {
    "silent": false,
    "enabled": false,
    "autoJobCancelation": true
  }
}
