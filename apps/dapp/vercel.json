{
  "rewrites": [{ "source": "/(.*)", "destination": "/" }],
  "builds": [{ "src": ".*", "use": "@vercel/static" }],  
  "github": {
    "silent": false,
    "enabled": false,
    "autoJobCancelation": true
  }
}
