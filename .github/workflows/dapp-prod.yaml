name: dapp => prod
on:
  push:
    branches:
      - 'main'
    paths:
      - "apps/dapp/**"
      - ".github/workflows/dapp-prod.yaml"

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.DAPP_PROJECT_ID }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Deploy to Vercel
        id: deploy_step
        run: |
          set -euo pipefail
          vcl() { npx --yes vercel@52.0.0 -A apps/dapp/vercel.json "$@"; }
          vcl pull --yes --environment=production
          vcl build --prod
          DEPLOYMENT_URL=$(vcl deploy --prebuilt --prod)
          echo "deployment_url=$DEPLOYMENT_URL" >> $GITHUB_OUTPUT
          vcl alias set "$DEPLOYMENT_URL" "templedao.link" --scope "$VERCEL_ORG_ID"
        continue-on-error: true
      - name: "notify failure"
        if: steps.deploy_step.outcome == 'failure'
        uses: rjstone/discord-webhook-notify@v1
        with:
          severity: error
          description: (Dapp) PROD Deploy to Vercel failed!
          details: Check the GH Action or Vercel logs for details.
          webhookUrl: ${{ secrets.DISCORD_WEBHOOK_URL }}
      - name: "fail"
        if: steps.deploy_step.outcome == 'failure'
        run: exit 1
      - name: "notify success"
        uses: rjstone/discord-webhook-notify@v1
        with:
          severity: info
          description: (Dapp) PROD Deploy to Vercel succeeded!
          details: "[View Prod Deployment](https://templedao.link)"
          webhookUrl: ${{ secrets.DISCORD_WEBHOOK_URL }}
