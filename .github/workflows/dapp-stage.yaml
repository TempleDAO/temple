name: dapp => stage
on:
  push:
    branches:
      - 'stage'
    paths:
      - "apps/dapp/**"
      - ".github/workflows/dapp-stage.yaml"

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.DAPP_PROJECT_ID }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Deploy to Vercel
        id: deploy_step
        run: |
          npx vercel@latest pull --yes --environment=preview -A apps/dapp/vercel.json
          npx vercel@latest build -A apps/dapp/vercel.json
          DEPLOYMENT_URL=$(npx vercel@latest deploy --prebuilt -A apps/dapp/vercel.json | tail -1)
          echo "deployment_url=$DEPLOYMENT_URL" >> $GITHUB_OUTPUT
          [ -n "$DEPLOYMENT_URL" ] && npx vercel@latest alias set "$DEPLOYMENT_URL" "stage.templedao.link" --scope "$VERCEL_ORG_ID" || exit 1
        continue-on-error: true
      - name: "notify failure"
        if: steps.deploy_step.outcome == 'failure'
        uses: rjstone/discord-webhook-notify@v1
        with:
          severity: error
          description: (Dapp) STAGE Deploy to Vercel failed!
          details: Check the GH Action or Vercel logs for details.
          webhookUrl: ${{ secrets.DISCORD_WEBHOOK_URL }}
      - name: "fail"
        if: steps.deploy_step.outcome == 'failure'
        run: exit 1
      - name: "notify success"
        uses: rjstone/discord-webhook-notify@v1
        with:
          severity: info
          description: (Dapp) STAGE Deploy to Vercel succeeded!
          details: "[View Stage Deployment](https://stage.templedao.link)"
          webhookUrl: ${{ secrets.DISCORD_WEBHOOK_URL }}
